1. sudo apt update
2. sudo apt install –y build-essential
3. nano overflow.c
#include <stdio.h>
#include <string.h>

int main(void)
{
    char buff[15];
    int pass = 0;

    printf("\n Enter the password : \n");
    gets(buff);

    if (strcmp(buff, "thegeekstuff"))
    {
        printf("\n wrong  Password \n");
 
    }
    else
    {
        printf("\n correct Password \n");
        pass = 1;
    }

    if (pass)
    {
        
        /* Now Give root or admin rights to user*/
        printf("\n Root privileges given to the user \n");
    }
    
    return 0;
}
ctrl+o --> enter --> ctrl+x

4. gcc -g -O0 -fno-stack-protector -no-pie -Wno-error=implicit-function-declaration overflow.c -o overflow

5. ./overflow
